name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      redis:
        image: redis:alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
      
      postgres:
        image: postgres:13-alpine
        env:
          POSTGRES_USER: kid
          POSTGRES_PASSWORD: 941005
          POSTGRES_DB: fund_daily
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov pytest-mock
    
    - name: Run tests
      env:
        FUND_DAILY_DB_TYPE: postgres
        FUND_DAILY_DB_HOST: localhost
        FUND_DAILY_DB_PORT: 5432
        FUND_DAILY_DB_NAME: fund_daily
        FUND_DAILY_DB_USER: kid
        FUND_DAILY_DB_PASSWORD: 941005
        FUND_DAILY_REDIS_URL: redis://localhost:6379/0
        FUND_DAILY_SECRET_KEY: test-secret-key-for-ci
        FUND_DAILY_JWT_SECRET: test-jwt-secret-key-for-ci-at-least-32-chars
      run: |
        pytest tests/ -v --cov=src --cov-report=xml \
          --ignore=tests/test_async_db.py \
          --ignore=tests/test_fund_database.py \
          --ignore=tests/test_fund_db_e2e.py
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        fail_ci_if_error: false
    
    - name: Run linting
      run: |
        pip install flake8 black isort
        flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
        black --check src/
        isort --check-only src/
  
  build:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Build frontend
      run: |
        cd web/vue3
        npm ci
        npm run build
        cd ../..
        mkdir -p frontend_dist
        mv dist/* frontend_dist/
        mkdir -p dist/frontend
        cp -r frontend_dist/* dist/frontend/
        rm -rf frontend_dist
    
    - name: Create deployment package
      run: |
        cp -r src dist/backend_src
        cp -r db dist/backend_db
        cp -r web/api_fastapi dist/api
        cp web/api_fastapi/main.py dist/
        cp requirements.txt dist/
        cp README.md dist/
        cp .env.example dist/.env
        
        # Create deployment script
        cat > dist/deploy.sh << 'EOF'
        #!/bin/bash
        echo "🚀 开始部署 Fund Daily..."
        
        # 检查Python版本
        python3 --version
        
        # 安装依赖
        pip3 install -r requirements.txt
        
        # 创建数据目录
        mkdir -p data
        
        # 启动服务
        echo "✅ 部署完成！"
        echo "启动服务: python3 main.py"
        EOF
        
        chmod +x dist/deploy.sh
    
    - name: Upload artifact (with retry)
      uses: actions/upload-artifact@v4
      with:
        name: fund-daily-deployment
        path: dist/
        retention-days: 5
        upload-interval: 10s
